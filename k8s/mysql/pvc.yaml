apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: immigration-support-system

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi
